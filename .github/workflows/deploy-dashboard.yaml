on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

  workflow_dispatch:

jobs:
  delay-1:
    runs-on: ubuntu-latest
    steps:
      - name: sleep
        run: |
          sleep $((RANDOM % 60))

  delay-2:
    runs-on: ubuntu-latest
    steps:
      - name: sleep
        run: |
          sleep $((RANDOM % 60))

  delay-3:
    runs-on: ubuntu-latest
    steps:
      - name: sleep
        run: |
          sleep $((RANDOM % 60))

  long-running:
    runs-on: ubuntu-latest
    steps:
      - name: sleep
        run: |
          sleep 120000
