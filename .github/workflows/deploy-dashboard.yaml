on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

  workflow_dispatch:

jobs:
  deploy:
    permissions:
      issues: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        run: |
          git --version
